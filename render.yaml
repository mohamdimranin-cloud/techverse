services:
  - type: web
    name: techverse-server
    runtime: node
    rootDir: server
    buildCommand: npm install --legacy-peer-deps
    startCommand: node index.js
    envVars:
      - key: NODE_ENV
        value: production
